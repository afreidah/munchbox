# Phlebotomy Game

Web-based flashcard application for studying blood collection tube order of
draw. Supports multiple game modes including quiz, drag-and-drop ordering,
and memory matching. Tube data is injected via a Nomad template from the
local `tubes.json` file, allowing customization without rebuilding the image.
