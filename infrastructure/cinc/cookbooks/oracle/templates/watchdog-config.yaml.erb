# -------------------------------------------------------------------------------
# Oracle Watchdog Monitor Configuration
#
# Managed by chef (oracle::watchdog) -- do not edit by hand. The body is
# intentionally empty; oracle-watchdog runs with its defaults + the env
# vars supplied via the systemd drop-in override. Add keys here only
# when overriding a default value.
# -------------------------------------------------------------------------------
