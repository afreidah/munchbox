{
  "service": {
    "name": "zfswatcher",
    "tags": ["traefik.enable=false"],
    "port": <%= @port %>,
    "check": {
      "tcp": "<%= @check_host %>:<%= @port %>",
      "interval": "30s",
      "timeout": "5s"
    }
  }
}
