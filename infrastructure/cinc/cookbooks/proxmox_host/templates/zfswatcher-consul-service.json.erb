{
  "service": {
    "name": "zfswatcher",
    "tags": ["traefik.enable=false"],
    "port": <%= @port %>,
    "check": {
      "http": "http://<%= @check_host %>:<%= @port %>/",
      "interval": "30s",
      "timeout": "5s"
    }
  }
}
