# frozen_string_literal: true

# -------------------------------------------------------------------------------
# Cookbook:: nvidia
# Recipe:: default
#
# Header-only; this cookbook is opted into via `recipe[nvidia::install]` in a
# per-node role (only the GPU host today).
# -------------------------------------------------------------------------------
