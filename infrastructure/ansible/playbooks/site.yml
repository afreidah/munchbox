---
# -------------------------------------------------------------------------------
# Main Site Playbook
#
# Project: Munchbox / Author: Alex Freidah
#
# Master playbook orchestrating complete infrastructure setup. Assumes Proxmox
# is already installed from ISO on bare metal and VMs are provisioned.
# -------------------------------------------------------------------------------

- name: Configure Proxmox cluster
  import_playbook: proxmox-cluster.yml
  tags: [proxmox, cluster]

- name: Install and configure Vault
  import_playbook: vault-setup.yml
  tags: [vault]

- name: Configure Nomad cluster
  import_playbook: nomad-cluster.yml
  tags: [nomad, cluster]
