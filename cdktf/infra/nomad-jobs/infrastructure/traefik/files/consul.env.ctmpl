# -----------------------------------------------------------------------------
# Consul Token (from Vault) — exported into env for the task
# -----------------------------------------------------------------------------
{{ with secret "kv/data/traefik" }}
CONSUL_TOKEN={{ .Data.data.consul_token }}
{{ end }}
